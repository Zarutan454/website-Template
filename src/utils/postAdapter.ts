// TODO: Diese Datei wurde auf Django-API migriert. Supabase-Logik wurde entfernt.
// Platzhalter-Implementierung, falls noch benötigt.

export function adaptPost(post: unknown) {
  // Optionally add a type guard here if you expect a certain shape
  // Passe das Post-Objekt an das UI-Format an
  return post;
}
